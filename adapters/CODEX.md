# Runtime Adapter — Codex

## Load
Make `DESIGN.md`, `visual-dna.json`, and `SKILL.md` available in the project.

## Run instruction
Read `SKILL.md` first, then `DESIGN.md`, then `visual-dna.json`. Execute the workflow exactly. Do not invent new brand rules.

INPUT:
- idea: [IDEA]
- objective: [OBJECTIVE]
- audience: [AUDIENCE]
- format: [FORMAT]

Return the output contract defined in `SKILL.md`.
